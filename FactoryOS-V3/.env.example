# FactoryOS V3 — environment template (Gate 1 öncesi placeholder)
# Copy to .env and fill values. Never commit .env

NODE_ENV=development
PORT=3001

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=factoryos
DB_PASSWORD=change_me
DB_NAME=factoryos_v3

SESSION_SECRET=change_me_use_long_random_string

# Optional: V2 database read-only reference during migration analysis only
# V2_DB_NAME=fabrika_erp_v2
